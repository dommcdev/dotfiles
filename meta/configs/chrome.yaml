
- link:
    ~/.config/chrome-flags.conf:
      path: apps/chrome/chrome-flags.conf
      if: '[ "$(uname)" = "Linux" ]'

- shell:
  - if [ "$(uname)" = "Darwin" ]; then brew install --cask google-chrome; else yay -S --noconfirm --needed --answerdiff None --answerclean None google-chrome; fi
