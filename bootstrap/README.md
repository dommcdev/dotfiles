# Bootstrap

Bootstrap scripts and iso auto-install configs for setting up my arch/mac/ubuntu machines.

Download and run the scripts using curl, e.g.
```bash
curl -fsSL files.dommcdev.net/arch -o install
```
