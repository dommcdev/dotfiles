- name: Ensure local bin exists
  ansible.builtin.file:
    path: "{{ home }}/.local/bin"
    state: directory
    mode: "0755"

- name: Link top-level scripts
  ansible.builtin.file:
    src: "{{ item }}"
    dest: "{{ home }}/.local/bin/{{ item | basename }}"
    state: link
    force: true
  loop: "{{ lookup('ansible.builtin.fileglob', root + '/scripts/*', wantlist=True) | reject('match', '.*/(utilities|archived)$') | list }}"

- name: Link utility scripts
  ansible.builtin.file:
    src: "{{ item }}"
    dest: "{{ home }}/.local/bin/{{ item | basename }}"
    state: link
    force: true
  loop: "{{ lookup('ansible.builtin.fileglob', root + '/scripts/utilities/*', wantlist=True) }}"

- name: Link sys helper
  ansible.builtin.file:
    src: "{{ root }}/sys"
    dest: "{{ home }}/.local/bin/sys"
    state: link
    force: true
