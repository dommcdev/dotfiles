# Bootstrap

Bootstrap scripts and iso auto-install configs for setting up my arch/mac/ubuntu machines.

Download the scripts using curl, e.g.
```bash
https://raw.githubusercontent.com/dommcdev/bootstrap/main/bootstrap-arch
```
Push updates from my private dotfiles repo:
```bash
git subtree push --prefix=public bootstrap main
